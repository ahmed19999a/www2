name: Build Android WebView Pro APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'android-webview-pro-v2/**'
      - '.github/workflows/build-android-webview-pro-v2.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK packages
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build APK
        working-directory: android-webview-pro-v2
        run: gradle --no-daemon assembleDebug
      - name: Rename APK
        run: cp android-webview-pro-v2/app/build/outputs/apk/debug/app-debug.apk AlMonjezAlSaree-WebView-Pro.apk
      - uses: actions/upload-artifact@v4
        with:
          name: AlMonjezAlSaree-WebView-Pro-APK
          path: AlMonjezAlSaree-WebView-Pro.apk
          retention-days: 30
